	我现在数据库中设计8张表：

第一张表是User表用于用户的注册信息，包括：id，email，password，余额，历史消耗（钱），connect(已连接的apikey_id)

第二张表是中转站表，包含：id，url，创建时间，修改时间。

第三张是APIkey表，包含：id, url_id, apikey,status，创建时间，修改时间。

第四张是订单表，包含：id, user_id，商品名称，支付方式，所需金额，实付金额，订单号，状态，优惠券id，创建时间，修改时间。

第五张表是优惠券表，包含：id,金额,描述，user_id，创建时间。

第六张表是日志表，在log-sync-polling.md里面

第7张和第8张是dashboard-tables.md里面的两张表用于看板。



​	看完我的三个项目之后，我们现在需要在kod-ai-portal前端中添加钱包界面点击之后加上/wallet路径跳转，界面和new-api项目的钱包前端界面一样，并且要给kod-ai-portal后端添加上对应的调用接口，调用字段也要相同，后端调用外部的支付逻辑相同，并且接口也要相同。此时我们就在官网有了钱包的页面，后端然后也能调外部支付。kod-ai-portal前端中也要添加一个数据看板和newapi中的展示一样，点击数据看板后，相关数据从数据库第7张和第8张的表里面获取，并且展示到前端。

​	kod的客户端中也就是我们点击新对话后的页面的选择模型的左边要加上能够选择url的列表，这个列表就来自于中转站表，鼠标点击某个中转站右边会有另一个框，里面是API密钥的id，前端可以将密钥以及对应id保存起来，以便后面调用url和apikey。如果某个密钥的status是1那我们前端显示一个红点（表示正在被占用）status为0 的表示未被占用然后被占用的放在最底部。我们会初始化数据库url以及url对应的几个密钥。

​	我们用户在客户端发送请求前，首先会选择某一个url和对应url下的某一个绿色标记的apikey_id（也就是某个apikey）就会发送请求给后端数据库查看用户的connect字段有没有值，没值就将apikey的status设置为1，并且将apikey_id设置到connect里面。有值那就将connect里的apikey_id拿到然后去apikey表将其status设置为0，然后将新的apikey写入connect。用户会输入内容。然后查询用户点击发送后首先在user表里面查询余额是否>0，小于0则返回余额不足，请充值；如果大于0则根据url和apikey发送请求。后续获取日志在log-sync-polling.md里面。然后根据日志里面的来定时统计到第七张和第八张表里面供看板调用使用。但是日志里面的消耗金额需要及时到user表里面扣除。

你需要设计相关接口以及各种字段，写完一定要一一核实kod-ai-portal前后端接口，kod客户端的请求以及newapi相关的接口。最后写一个md文件给我也就是需求文档，每个接口都需要设计好！！！尽量不要修改原生接口。



