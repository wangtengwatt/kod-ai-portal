# 本地开发环境
# 数据源与 Redis 沿用 application.yml 的 loopback 默认值，如需其他本地实例用环境变量覆盖：
#   MYSQL_HOST / MYSQL_PORT / MYSQL_DATABASE / MYSQL_USERNAME / MYSQL_PASSWORD
#   REDIS_HOST / REDIS_PORT / REDIS_PASSWORD
logging:
  level:
    com.kod: debug
